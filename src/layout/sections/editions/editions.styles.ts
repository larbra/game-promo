import styled from 'styled-components'
import { SectionHeading } from 'components/common/section-heading'

const Editions = styled.section`
    padding-top: 8rem;
`

const EditionsHeading = styled(SectionHeading)`
    margin-bottom: 3.5rem;
`

export const S = {
    Editions,
    EditionsHeading,
}
