import styled from 'styled-components'
import { SectionHeading } from 'components/common/section-heading'

const FAQ = styled.section`
    padding-top: 10rem;
`

const FAQHeading = styled(SectionHeading)`
    margin-bottom: 3.7rem;
`

export const S = {
    FAQ,
    FAQHeading,
}
