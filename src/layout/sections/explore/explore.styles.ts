import styled from 'styled-components'
import { SectionHeading } from 'components/common/section-heading'

const Explore = styled.section`
    padding-top: 8.8rem;
    overflow: hidden;
`

const ExploreHeading = styled(SectionHeading)`
    margin-bottom: 3.2rem;
`

export const S = {
    Explore,
    ExploreHeading,
}
